    /* Ring Buffer'da kalan artık karakterleri (Enter tuşu vb.) temizle */
    uint8_t dummy;
    while(Ring_Buffer_Read(&dummy) == 0); // Havuz boşalana kadar oku
    file_started = 0; // Bayrağı sıfırla
    
    printf("Simdi dosyayi surukleyin... (Bekleniyor)\r\n");
